version: "1.0"

# Agent repository sources
agent_sources:
  - id: "awesome-repo-configs"
    url: "https://raw.githubusercontent.com/Chat2AnyLLM/awesome-repo-configs/main/agent_repos.json"
    enabled: true
    priority: 1
    timeout: 30

# Generation settings
generation:
  output_file: "README.md"

# Logging settings
logging:
  level: "INFO"
